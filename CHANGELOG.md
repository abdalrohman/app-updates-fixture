# Changelog — v1.1.0

> This body is the welcome-screen changelog. The service prints it verbatim.

## Added

* New welcome gallery (hero, banner, avatar) served as release assets.
* Images are cached: re-downloaded only when changed or new.

## Fixed

* Faster cold start: update check moved off the UI thread.

## Assets in this release

* `myapp-v1.1.0.exe` (placeholder text binary for the demo)
* `SHA256SUMS.txt` (hash gate)
* `images-manifest.json` + 3 PNGs (welcome screen)
